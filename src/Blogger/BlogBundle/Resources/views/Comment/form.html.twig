<form action="{{ path('BloggerBlogComment_create', { 'blog_id' : comment.blog.id } ) }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <input type="submit" value="Submit">
    </p>
</form>